# 코코알바 — 작업 기록 SOP
**작업일:** 2026-03-19
**프로젝트:** `p2. 브랜드_통합_시스템`
**작성:** Claude (AI 페어 프로그래밍)
**공유 대상:** 안티그래비티 포함 전 팀원

---

## 목차

1. [블루 → 핫핑크 전사 전환 (커뮤니티)](#1-블루--핫핑크-전사-전환-커뮤니티)
2. [그녀들의수다 헤더 아이콘·타이틀 색상 수정](#2-그녀들의수다-헤더-아이콘타이틀-색상-수정)
3. [HeroSection 그라데이션 순서 변경](#3-herosection-그라데이션-순서-변경)
4. [인재정보 퀵메뉴 로그인 루프 버그 수정](#4-인재정보-퀵메뉴-로그인-루프-버그-수정)
5. [my-shop 무한 setState 루프 버그 수정](#5-my-shop-무한-setstate-루프-버그-수정)
6. [Footer 관리자쪽지 → MainHeader 모달 연동](#6-footer-관리자쪽지--mainheader-모달-연동)
7. [이용약관·개인정보처리방침·청소년보호정책 업데이트](#7-이용약관개인정보처리방침청소년보호정책-업데이트)
8. [고객센터 번호 전체 교체 (1544-5568 → 1877-1442)](#8-고객센터-번호-전체-교체)
9. [고객지원센터 블루톤 전체 핫핑크 변환](#9-고객지원센터-블루톤-전체-핫핑크-변환)
10. [고객지원센터 타이틀 CS 아이콘 블루 수정](#10-고객지원센터-타이틀-cs-아이콘-블루-수정)
11. [HeroSection 무료광고 버튼 → PaymentPopup 연동](#11-herosection-무료광고-버튼--paymentpopup-연동)
12. [가이드 시작하기 버튼 → 업체회원 전용 가드 추가](#12-가이드-시작하기-버튼--업체회원-전용-가드-추가)
13. [Step4Extras 중복 키 에러 수정](#13-step4extras-중복-키-에러-수정)

---

## 브랜드 컬러 기준 (전팀 공통)

> **핫핑크 기준색: `#f82b60`**
> Tailwind 블루 계열(`blue-500`, `blue-600` 등)을 모두 이 색으로 통일하는 것이 현재 세션의 핵심 정책.
> - `bg-blue-600` → `bg-[#f82b60]`
> - `text-blue-600` → `text-[#f82b60]`
> - `bg-blue-50` → `bg-rose-50`
> - `border-blue-xxx` → `border-[#f82b60]` 또는 `border-rose-xxx`
> - `shadow-blue-xxx` → `shadow-rose-xxx`
> - hover 시 어두운 핑크: `#db2456`

---

## 1. 블루 → 핫핑크 전사 전환 (커뮤니티)

**배경:** 캡처 확인 후 커뮤니티 전 페이지의 블루 계열을 핫핑크로 일괄 교체 요청.

### 파일 1: `src/app/community/CommunityContent.tsx`

| 위치 | 변경 전 | 변경 후 |
|------|---------|---------|
| 히어로 배너 배경 | `from-blue-xxx` | `from-[#f82b60] to-rose-400` |
| 카테고리 활성 탭 | `border-blue-500 text-blue-600` | `border-[#f82b60] text-[#f82b60]` |
| 글쓰기 버튼 | `bg-blue-600` | `style={{ backgroundColor: '#f82b60' }}` |
| 좋아요·댓글 아이콘 | `text-blue-500` | `style={{ color: '#f82b60' }}` |
| 로그인/기업 모달 | 블루 계열 | 핫핑크 계열 |

### 파일 2: `src/app/community/[id]/CommunityDetailClient.tsx`

| 위치 | 변경 전 | 변경 후 |
|------|---------|---------|
| 댓글 말풍선 아이콘 (`MessageSquare`) | `text-blue-500` | `style={{ color: '#f82b60' }}` |
| 목록으로 돌아가기 버튼 | 블루 테두리 | `style={{ borderColor: '#f82b60', color: '#f82b60' }}` |
| 비밀번호 모달 확인 버튼 | `bg-blue-600` | `style={{ backgroundColor: '#f82b60' }}` |
| 아바타, 등록 버튼 | 블루 계열 | 핫핑크 계열 |

> **⚠️ 주의:** 댓글 아이콘은 초기 batch에서 누락되어 별도로 추가 수정했음.
> 이후 신규 컴포넌트 작성 시 댓글 관련 아이콘에 블루가 기본값으로 들어가지 않도록 주의.

### 파일 3: `src/app/community/write/page.tsx`

| 위치 | 변경 전 | 변경 후 |
|------|---------|---------|
| 카테고리 라벨 | `text-blue-600` | `style={{ color: '#f82b60' }}` |
| 활성 카테고리 버튼 | `bg-blue-600` | `style={{ backgroundColor: '#f82b60' }}` |
| 비밀글 체크박스 | `peer-checked:border-blue-500` | `peer-checked:border-[#f82b60]` |
| 사진첨부 hover | `hover:border-blue-200` | `hover:border-rose-200` |
| 안내 문구 (비밀번호 필요) | `text-blue-600` | `text-[#f82b60]` |
| 등록 버튼 | `bg-blue-600` | `style={{ backgroundColor: '#f82b60' }}` |

---

## 2. 그녀들의수다 헤더 아이콘·타이틀 색상 수정

**파일:** `src/components/common/MainHeader.tsx`

**문제:** 좌상단 말풍선 아이콘(`MessageCircle`)에 파란 테두리(stroke)가 남아 있었고, "그녀들의수다" 타이틀 텍스트가 기본 회색이었음.

**조치:**
```tsx
// 수정 전
<MessageCircle size={24} className="text-blue-500" />
<span className="text-lg font-black text-gray-900">그녀들의수다</span>

// 수정 후
<MessageCircle size={24} className="fill-[#f82b60]" style={{ color: '#f82b60' }} />
<span className="text-lg md:text-xl font-black tracking-tight" style={{ color: '#f82b60' }}>
    그녀들의수다
</span>
```

> `fill-[#f82b60]`을 추가하여 내부 채움색을 설정하고, `color`로 stroke도 같은 색으로 통일하면 파란 테두리가 완전히 제거됨.

---

## 3. HeroSection 그라데이션 순서 변경

**파일:** `src/components/home/HeroSection.tsx`

**변경 내용:**

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 타이틀 그라데이션 | `from-blue-400 via-rose-300 to-[#f82b60]` (블루→핑크) | `from-[#f82b60] via-rose-300 to-blue-400` (핑크→블루) |
| 배지 배경·테두리 | `bg-blue-xxx / border-blue-xxx` | `bg-[#f82b60]/10 / border-[#f82b60]/30` |
| 우측 accent blob | `bg-blue-600/20` | `bg-[#f82b60]/10` |

---

## 4. 인재정보 퀵메뉴 로그인 루프 버그 수정

**파일:** `src/middleware.ts`

**문제:**
메인페이지 퀵메뉴에서 `/talent` 클릭 시 로그인이 되어 있는데도 로그인 화면으로 튕기는 현상.

**원인:**
미들웨어의 `PROTECTED_AUTH_PATHS`에 `/talent`가 포함되어 있었고, 개발 환경에서는 Supabase 쿠키 기반 세션이 없어 인증 실패 처리됨.

**조치:**
```ts
// 수정 전
if (needsAuth) {

// 수정 후
if (needsAuth && process.env.NODE_ENV !== 'development') {
```

**패턴:** 이미 `/admin` 경로에서 동일한 `NODE_ENV !== 'development'` 우회 패턴을 사용 중. 일관성 유지.

> **⚠️ 배포 전 주의:** 프로덕션에서는 이 조건이 활성화되어 Supabase 세션 쿠키 없는 접근은 로그인 페이지로 리다이렉트됨. 운영 환경에서는 정상 동작.

---

## 5. my-shop 무한 setState 루프 버그 수정

**파일:** `src/app/my-shop/page.tsx`

**문제:**
`useEffect` deps 배열에 내부 함수 또는 객체 참조가 포함되어 매 렌더마다 재실행 → 무한 setState 루프 발생.

**조치:**
```tsx
// 수정 전 (함수/객체 deps 포함 → 매 렌더 재실행)
useEffect(() => { fetchData(); }, [authUser, fetchData]);

// 수정 후 (원시값만 deps에 유지)
useEffect(() => { fetchData(); }, [authUser?.id]);
```

**원칙:** `useEffect` deps 배열에는 `authUser?.id` 같은 원시값(string/number)만 넣을 것. 함수나 객체 전체를 deps에 넣으면 매 렌더마다 새 참조로 인식되어 루프 발생.

---

## 6. Footer 관리자쪽지 → MainHeader 모달 연동

**파일:**
- `src/components/layout/Footer.tsx`
- `src/components/common/MainHeader.tsx` (기존 `open-note-modal` 리스너 활용)

**문제:**
Footer의 "관리자쪽지" 항목이 `<Link href="#">`로 연결되어 있어 동작하지 않았음.

**조치:**
```tsx
// Footer: Link → button 으로 변경
<button
    onClick={() =>
        window.dispatchEvent(
            new CustomEvent('open-note-modal', { detail: { receiver: '관리자' } })
        )
    }
>
    관리자쪽지
</button>
```

**동작 원리:**
MainHeader에 기존에 `open-note-modal` CustomEvent 리스너가 등록되어 있음.
Footer에서 이벤트를 dispatch하면 MainHeader의 `MessageModal`이 `receiver: '관리자'`로 열림.

---

## 7. 이용약관·개인정보처리방침·청소년보호정책 업데이트

**파일:** `src/app/customer-center/page.tsx` (약관 및 정책 탭)

**변경 내용 요약:**

| 항목 | 변경 내용 |
|------|----------|
| 이용약관 | 주요 조항 세부화 (금지 행위, 계정 정지 사유, 분쟁 해결 조항 등 강화) |
| 개인정보처리방침 | 수집 항목, 보유 기간, 제3자 제공, 파기 절차 등 상세화 |
| 청소년보호정책 | 청소년 유해정보 차단 기준, 모니터링 방법 등 구체화 |
| 공통 (약관 하단) | **책임자:** 김대순, 고남우 / **연락처:** 1877-1442 / **이메일:** bizsetter7@gmail.com |

---

## 8. 고객센터 번호 전체 교체

**기존:** `1544-5568` → **변경:** `1877-1442`
**운영시간 추가:** 평일 10:00~18:00 / 점심 12:00~13:00 / 주말·공휴일 휴무

**수정된 파일 목록:**

| 파일 | 비고 |
|------|------|
| `src/app/customer-center/page.tsx` | 사이드바, 센터홈, 공지상세, 각종 배너 |
| `src/components/LeftSidebar.tsx` | PC 좌측 사이드바 |
| `src/components/RightSidebar.tsx` | PC 우측 사이드바 |

> 수정 후 사이트 전체 `grep "1544-5568"` 결과 0건 확인 완료.

---

## 9. 고객지원센터 블루톤 전체 핫핑크 변환

**파일:** `src/app/customer-center/page.tsx`

수정 후 `grep "blue-"` 결과 0건 확인 완료.

**주요 변환 영역:**

| 영역 | 변환 내용 |
|------|----------|
| 사이드바 네비 활성 탭 | `bg-blue-50 border-blue-500 text-blue-600` → `bg-rose-50 border-[#f82b60] text-[#f82b60]` |
| 고객센터 PhoneCall 아이콘 박스 | `bg-blue-600` → `bg-[#f82b60]` |
| "공휴일/주말 휴무" 텍스트 | `text-blue-600` → `text-[#f82b60]` |
| 센터홈 히어로 accent blob | `bg-blue-600/20` → `bg-[#f82b60]/20` |
| 센터홈 히어로 h2 강조 | `text-blue-600` → `text-[#f82b60]` |
| 센터홈 "지금 문의하기" 버튼 | `bg-blue-600` → `bg-[#f82b60]` |
| 공지사항 수직 구분 바 | `bg-blue-600` → `bg-[#f82b60]` |
| 공지 카테고리 배지 (기타) | `bg-blue-600 text-white` → `bg-[#f82b60] text-white` |
| 광고안내 히어로 그라데이션 | `from-blue-500 to-blue-600` → `from-[#f82b60] to-rose-400` |
| 광고 티어 카드 hover/아이콘/버튼 | 블루 계열 → 핫핑크 계열 |
| 상세 단가표 헤더/가격 컬럼 | `text-blue-600` → `text-[#f82b60]` |
| 섹션 구분 바 (`w-2 h-8`) | `bg-blue-600` → `bg-[#f82b60]` (전역) |
| 이미지 가이드 확대 버튼 | `bg-blue-600` → `bg-[#f82b60]` |
| 디자인 가이드 CTA 버튼 | `bg-blue-600 hover:bg-blue-700` → `bg-[#f82b60] hover:bg-[#db2456]` |
| 지역 기반 매칭 그라데이션 | `from-blue-600 to-blue-500` → `from-[#f82b60] to-rose-400` |
| 1:1 문의 관련 요소 전체 | 블루 계열 → 핫핑크 계열 |
| ExposureItem DELUXE·SPECIAL | `bg-blue-600` badge → `bg-[#f82b60]` |
| ResumeNoticeDetail 가이드 박스 | `bg-blue-600` → `bg-[#f82b60]` |
| CardPaymentNoticeDetail 아이콘 박스 | `bg-blue-50 border-blue-100` → `bg-rose-50 border-rose-100` |

---

## 10. 고객지원센터 타이틀 CS 아이콘 블루 수정

**파일:** `src/components/common/MainHeader.tsx`

**문제:** `/customer-center` 경로 진입 시 헤더 좌측 "CS" 뱃지가 `bg-blue-600`으로 표시됨.

**조치:**
```tsx
// 수정 전
<div className="w-7 h-7 bg-blue-600 rounded-lg flex items-center justify-center">

// 수정 후
<div className="w-7 h-7 bg-[#f82b60] rounded-lg flex items-center justify-center">
```

**추가 수정:** 모바일 메뉴 활성 탭 색상도 함께 변경.
```tsx
// 수정 전
'bg-blue-50 text-blue-600'
// 수정 후
'bg-rose-50 text-[#f82b60]'
```

---

## 11. HeroSection 무료광고 버튼 → PaymentPopup 연동

**파일:** `src/components/home/HeroSection.tsx`

**문제:** "무료로 광고 올리기 🚀" 버튼이 `/ad-apply`로 연결되어 있었으나, 해당 라우트가 없어 404 에러 발생.

**조치:**
- `PaymentPopup` 컴포넌트 import 추가
- `isPaymentPopupOpen` 상태 추가
- `<Link href="/ad-apply">` → `<button onClick={() => setIsPaymentPopupOpen(true)}>` 로 변경
- JSX 하단에 `<PaymentPopup>` 렌더링 추가

```tsx
// 변경 후
import { PaymentPopup } from '@/components/home/PaymentPopup';

const [isPaymentPopupOpen, setIsPaymentPopupOpen] = useState(false);

// 버튼
<button onClick={() => setIsPaymentPopupOpen(true)} ...>
    무료로 광고 올리기 🚀
</button>

// 팝업
<PaymentPopup
    isOpen={isPaymentPopupOpen}
    onClose={() => setIsPaymentPopupOpen(false)}
    initialTier="grand"
/>
```

**결과:** 버튼 클릭 시 "사장님 전용 상품 안내" 팝업이 `grand` 티어 기본 선택으로 열림.

---

## 12. 가이드 시작하기 버튼 → 업체회원 전용 가드 추가

**파일:** `src/app/guide/page.tsx`

**문제:** "시작하기" 버튼이 `/my-shop?view=form&new=true`로 바로 이동. 개인회원도 접근 가능.

**조치:** `useAuth` 로 회원 타입 체크 후 분기 처리.

```tsx
const { user, isLoggedIn } = useAuth();

const handleStart = () => {
    if (!isLoggedIn) {
        alert('로그인이 필요한 서비스입니다.');
        router.push('/?page=login');
        return;
    }
    if (user?.type !== 'business') {
        alert('업체회원만 이용 가능한 서비스입니다.\n업체회원으로 가입 후 이용해주세요!');
        return;
    }
    router.push('/my-shop');
};
```

| 상태 | 동작 |
|------|------|
| 미로그인 | 로그인 필요 안내 → `/?page=login` 이동 |
| 개인회원 | "업체회원만 이용 가능합니다!" 알림 표시 |
| 업체회원 | `/my-shop` (마이페이지) 이동 |

**추가:** 가이드 페이지 내 나머지 블루 색상도 핫핑크로 통일.
- `bg-blue-50 text-blue-600` → `bg-rose-50 text-[#f82b60]`
- `border-blue-200`, `text-blue-500`, `text-blue-600` 전부 핫핑크 계열로 변경

---

## 13. Step4Extras 중복 키 에러 수정

**파일:** `src/app/my-shop/constants.ts`

**에러 메시지:**
```
Encountered two children with the same key, `엔터프라이즈 인재 솔루션`.
Keys should be unique so that components maintain their identity across updates.
```

**원인:** `STEP4_CONVENIENCE_KEYWORDS` 배열과 `FORBIDDEN_WORDS` 배열 모두에서 `'엔터프라이즈 인재 솔루션'`이 중복 삽입되어 있었음.

**조치:**
```ts
// 수정 전
export const STEP4_CONVENIENCE_KEYWORDS = [
    ...,
    '엔터프라이즈 인재 솔루션', '엔터프라이즈 인재 솔루션',  // ← 중복
    ...
];

export const FORBIDDEN_WORDS = [
    ...,
    '엔터프라이즈 인재 솔루션', '엔터프라이즈 인재 솔루션'   // ← 중복
];

// 수정 후 (각각 1개씩만 유지)
```

---

## 변경 파일 요약

| 파일 | 변경 유형 |
|------|----------|
| `src/app/community/CommunityContent.tsx` | 블루→핫핑크 전환 |
| `src/app/community/[id]/CommunityDetailClient.tsx` | 블루→핫핑크 전환, 댓글 아이콘 수정 |
| `src/app/community/write/page.tsx` | 블루→핫핑크 전환 |
| `src/components/common/MainHeader.tsx` | 그녀들의수다 아이콘/타이틀, CS 배지, 모바일 메뉴 탭 |
| `src/components/home/HeroSection.tsx` | 그라데이션 순서, 무료광고 버튼 → PaymentPopup |
| `src/middleware.ts` | /talent 로그인 루프 버그 (NODE_ENV 우회) |
| `src/app/my-shop/page.tsx` | 무한 setState 루프 (useEffect deps 정리) |
| `src/components/layout/Footer.tsx` | 관리자쪽지 CustomEvent 연동 |
| `src/app/customer-center/page.tsx` | 블루→핫핑크 전체 전환, 고객센터 번호, 약관 업데이트 |
| `src/components/LeftSidebar.tsx` | 고객센터 번호 교체 |
| `src/components/RightSidebar.tsx` | 고객센터 번호 교체 |
| `src/app/guide/page.tsx` | 시작하기 버튼 업체회원 가드, 블루→핫핑크 |
| `src/app/my-shop/constants.ts` | 중복 키 제거 |

---

## 아키텍처 패턴 정리 (안티그래비티 참고)

### ① 컬러 적용 우선순위
```
1. Tailwind 클래스: bg-[#f82b60], text-[#f82b60], border-[#f82b60]
2. inline style: style={{ color: '#f82b60' }}  ← 동적 조건부 색상에 사용
3. 어두운 hover: #db2456 (약 10% 어둡게)
4. 밝은 배경: bg-rose-50, border-rose-100, shadow-rose-200
```

### ② 미들웨어 인증 패턴
```ts
// 개발 환경에서 Supabase 쿠키 인증 우회 패턴 (일관성 유지)
if (needsAuth && process.env.NODE_ENV !== 'development') {
    const sessionCookie = request.cookies.get('sb-...');
    if (!sessionCookie) return NextResponse.redirect(...);
}
```

### ③ 전역 이벤트 버스 패턴 (컴포넌트 간 통신)
```ts
// 발신 (Footer 등 하위 컴포넌트)
window.dispatchEvent(new CustomEvent('open-note-modal', { detail: { receiver: '관리자' } }));

// 수신 (MainHeader — 기존 등록된 리스너)
window.addEventListener('open-note-modal', handler);
```
> 컴포넌트 트리를 타지 않고 전역 통신이 필요할 때 사용. props drilling 방지.

### ④ useEffect 무한루프 방지
```tsx
// ❌ 잘못된 패턴 — 객체/함수는 매 렌더마다 새 참조
useEffect(() => { ... }, [authUser, fetchData]);

// ✅ 올바른 패턴 — 원시값만 deps에
useEffect(() => { ... }, [authUser?.id]);
```

---

## 미완료 / 참고사항

- **`/notice` 라우트:** 기존에 존재하던 `/notice` 경로는 사용하지 않는 것으로 확인. 혼란 방지를 위해 삭제 검토 권장.
- **이미지 파일 업로드:** `community/write`의 이미지 첨부는 현재 로컬 ObjectURL 방식 (Mock). Supabase Storage 연동 시 교체 필요.
- **Supabase 연결:** 고객센터 1:1문의 탭에서 `"Failed to fetch"` 에러는 Supabase 프로젝트 연결 시 자연 해결.
- **게시글 수정 기능:** `CommunityDetailClient.tsx`의 "수정하기" 기능은 현재 미구현 (`alert('준비 중')` 상태). 추후 별도 작업 필요.

---

*이 문서는 Claude AI가 페어 프로그래밍으로 작업한 내용을 자동 정리한 SOP입니다.*
*전 팀원(안티그래비티 포함) 공유용. 추가 작업 시 해당 파일에 섹션을 이어서 기록할 것.*
