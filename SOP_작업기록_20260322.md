# SOP 작업기록 — 2026-03-22
> P2 코코알바 브랜드 통합 시스템 | 작성자: Claude + 대표님
> 이 문서는 2026-03-22 세션의 전체 구현 내용, E2E 흐름, 설계 결정을 기록합니다.

---

## 1. 이번 세션 구현 완료 목록

### 1-1. 이미지 섹션 폰트 정규화

**문제**: Grand/Premium(AdBannerCard), Deluxe/Special(ShopCard)의 이미지 섹션 공고제목이 비대하게 비규칙적으로 표시됨

**해결**:
| 컴포넌트 | 이전 | 이후 |
|---------|------|------|
| AdBannerCard (Grand/Premium) | `text-xl md:text-2xl` | `text-[13px] line-clamp-5 w-full` |
| ShopCard showImage=true (Deluxe/Special) | `text-[15px]` | `text-[12px] line-clamp-4 w-full` |

**관련 파일**:
- `src/components/shop/AdBannerCard.tsx`
- `src/components/shop/ShopCard.tsx`

---

### 1-2. 급구/추천 카드 레이아웃 3-Row 재설계

**문제**: 이미지 없는 급구/추천 카드가 Deluxe/Special과 차별점이 없고, 세로 폭이 불필요하게 컸음

**해결**: 4단 구조(제목 1줄 + Row1 + Row2 + Row3)로 재설계

| 순서 | 내용 | 스타일 |
|-----|------|--------|
| 제목 | 공고제목 1줄 (`line-clamp-1`) + IconBadge + 형광펜 | `text-[12px] font-black`, `pl-6` (NEW배지 충돌방지) |
| Row 1 | 닉네임(좌) / 지역(우) | `text-[10px]` |
| Row 2 | 급여배지 + 급여액(좌) / 업종(우) | 배지 `16×16px`, 급여 `text-[12px]` |
| Row 3 | paySuffixes 최대 4개 (없으면 미표시) | `text-[9px]`, `max-h-[16px]` |

**관련 파일**: `src/components/shop/ShopCard.tsx`

---

### 1-3. 사이드바 고객지원센터 UI 개편

**변경 내용**:
- TALK(카카오) 버튼 **완전 제거**
- 전화번호: 라벨 제거 + `text-[22px] font-black text-purple-600 tracking-tighter`
- 텔레그램 문의 섹션 추가:
  - 공식 SVG 아이콘 (배경: `bg-[#2AABEE]` 원형)
  - 텍스트: "텔레그램 문의"
  - ID 표시: `@cocoalba` (텔레그램 컬러)
  - 링크: `https://t.me/cocoalba`
- 운영시간 텍스트 추가: 평일 10:00~18:00 / 점심 12:00~13:00 / 공휴일·주말 휴무

**관련 파일**: `src/components/LeftSidebar.tsx`

---

### 1-4. 고객지원센터 탭(TabHome) UI 개편

**변경 내용**: 캡처2 레퍼런스 기준으로 전면 재설계
- 전화 아이콘 박스: `w-14 h-14 bg-pink-500 text-white rounded-[18px]` + `PhoneCall size={26}`
- 고객센터 번호: `text-3xl font-black tracking-tighter` (1877-1442)
- 운영시간 상세 표기 (공휴일/주말 휴무 핫핑크 강조)
- 텔레그램 버튼: `bg-gray-900 hover:bg-gray-800 text-white px-6 py-4 rounded-2xl`
  - 링크: `https://t.me/cocoalba`
  - 공식 SVG 아이콘 포함

**관련 파일**: `src/app/customer-center/components/TabHome.tsx`

---

### 1-5. 공고 상세 모달 헤더 그라디언트 AD_TIER_STANDARDS 동기화

**문제**: JobDetailModal, AdDetailModal의 헤더 색상이 AD_TIER_STANDARDS와 불일치
- Grand: purple로 표시되고 있었음 (기준: amber)
- Premium: purple/blue로 표시 (기준: red)
- Recommended: indigo/violet (기준: orange)

**해결**:
| 파일 | 변경 항목 | 내용 |
|-----|---------|------|
| `JobDetailModal.tsx` | `getHeaderTheme()` | 전 티어 그라디언트 AD_TIER_STANDARDS 동기화 |
| `JobDetailModal.tsx` | `badge` 컬러 | `bg-xxx-100` → **`bg-white/20`** (전 티어 흰색 통일) |
| `AdDetailModal.tsx` | `TIER_GRADIENTS` | grand: purple→amber, recommended: amber→orange 등 수정 |
| `layout.ts` | `GRADIENTS` 상수 | 전 티어 `bg-gradient-to-br` + AD_TIER_STANDARDS 동기화 |

**관련 파일**:
- `src/components/jobs/JobDetailModal.tsx`
- `src/app/my-shop/components/AdDetailModal.tsx`
- `src/constants/layout.ts`

---

### 1-6. PAY_BADGE_STANDARDS v2.0 — 급여 배지 색상 전면 재정의

**문제**: 시급·일급·주급이 모두 blue 계열로 유사, 연봉·TC도 green 계열로 혼동

**해결**: 8종 완전 구별 색상으로 재정의 (2026-03-22 대표님 확정)

| 급여 종류 | 이전 | 이후 | 변경 사유 |
|---------|------|------|---------|
| 주급 | `bg-blue-500 (#3B82F6)` | `bg-green-500 (#22C55E)` | 시급·일급과 구분 |
| 연봉 | `bg-green-600 (#16A34A)` | `bg-red-500 (#EF4444)` | 주급과 구분 |
| TC | `bg-emerald-500 (#10B981)` | `bg-orange-500 (#F97316)` | 연봉과 구분 |

**관련 파일**:
- `src/constants/standards.ts` (PAY_BADGE_STANDARDS)
- `src/utils/payColors.ts` (TC 하드코딩 색상 동기화)

---

### 1-7. urgent 모달 헤더 버그 수정

**문제**: `urgent` tier 카드 클릭 시 모달 헤더가 Basic(stone) 색상으로 fallback됨
**원인**: `urgent`가 `AD_TIER_STANDARDS`의 `altId`에 없어 `find()` 실패 → `AD_TIER_STANDARDS[6]`(Basic) 반환

**수정** (`JobDetailModal.tsx`):
```ts
const isUrgentTier = pt.includes('urgent');
const tierStandard = isUrgentTier
    ? { id: 'urgent' }
    : (AD_TIER_STANDARDS.find(...) || AD_TIER_STANDARDS[6]);
// + case 'urgent': purple 추가
```

**관련 파일**: `src/components/jobs/JobDetailModal.tsx`

---

### 1-8. 급구/추천 등급 단일 보라색 통일

**결정**: 급구(urgent)와 추천(recommended)은 하나의 등급 → 단일 색상 통일

**색상 선정 과정**:
| 후보 | 기각 이유 |
|------|----------|
| 🟠 Orange | Grand(amber)와 유사 |
| 🌹 Rose | Premium(red)과 유사 |
| 🟣 **Purple** | **확정** — 기존 7개 등급 완전 미사용 |

**수정 파일**:
| 파일 | 변경 |
|------|------|
| `constants/standards.ts` | p5: `bg-orange-500` → `bg-purple-600` |
| `components/jobs/JobDetailModal.tsx` | p5·urgent: orange/rose → purple |
| `components/shop/ShopCard.tsx` | urgent·recommended: → purple |
| `components/BannerSidebar.tsx` | urgent·recommended: → purple |
| `app/my-shop/components/AdDetailModal.tsx` | urgent·recommended: → purple |
| `components/jobs/JobListView.tsx` | 급구 배지: `bg-red-600` → `bg-purple-600` |

---

### 1-9. shops.json bait 카드 tier 정정

**문제**: 급구/추천 섹션(slice(48,60))에 배치된 bait 카드들이 잘못된 tier 보유 → 모달 헤더 색상 불일치

**수정**: 7개 카드 tier → `urgent`로 일괄 변경

| id | 이전 | 이후 |
|----|------|------|
| bait-busan-06~10 | grand/special | urgent |
| bait-daegu-06, 08 | grand | urgent |

**관련 파일**: `src/lib/data/shops.json`

---

## 2. 설계 결정 사항

### 2-1. 단일 소스 원칙 강화
- `AD_TIER_STANDARDS` → `GRADIENTS`, `getHeaderTheme`, `TIER_GRADIENTS` 4곳 동시 동기화 의무화
- `PAY_BADGE_STANDARDS` → `payColors.ts` 완전 파생 구조 유지

### 2-2. 모달 배지 흰색 통일
- 헤더 배경이 이미 등급 컬러(그라디언트)이므로, 내부 섹션 배지는 `bg-white/20`으로 통일
- 가독성·일관성 동시 확보

### 2-3. 고객지원 채널 단순화
- 카카오톡(TALK) 제거 → 텔레그램 단일 메신저 채널로 집중
- 플랫폼 정체성에 맞는 단순하고 명확한 연락처 구조

---

## 3. 브라우저 검증 결과 (2026-03-22)

| 검증 항목 | 결과 |
|---------|------|
| Grand 모달 헤더 → amber | ✅ 확인 |
| 시급 배지 → cyan | ✅ 확인 |
| 사이드바 1877-1442 purple + 텔레그램 | ✅ 확인 |
| 급구/추천 3-Row 레이아웃 | ✅ 확인 |
| 급여 배지 v2.0 색상(주급/연봉/TC) | 코드 적용 완료, 해당 타입 데이터 화면 노출 대기 |

---

---

### 1-10. 공고등록 Step2 에디터 iOS 모바일 버그 수정

**문제**: iOS Safari에서 툴바 버튼 탭 시 `execCommand`가 포커스 없이 실행되어 볼드/이탤릭/정렬 등 서식이 미적용

**원인**: 버튼 `onMouseDown` 핸들러에서 `execCmd()` → `focus()` 순서 역전
- iOS Safari: `contenteditable`에 포커스 없으면 `execCommand` 무시됨

**해결**: 전체 버튼 핸들러를 `focus()` → `restoreSelection()` → `execCmd()` 순서로 변경

```tsx
// Before (모바일 버그)
onMouseDown={(e) => { e.preventDefault(); restoreSelection(); execCmd('bold'); editorRef.current?.focus(); }}
// After (iOS 호환)
onMouseDown={(e) => { e.preventDefault(); editorRef.current?.focus(); restoreSelection(); execCmd('bold'); }}
```

**수정 버튼**: Bold, Italic, Underline, 정렬3종, 폰트, 텍스트색상, 하이라이트, 이모지 (총 9종)

**관련 파일**: `src/app/my-shop/components/form/steps/Step2JobDetail.tsx`

---

### 1-11. saveSelection() cloneRange() 누락 버그 수정

**문제**: `saveSelection()`에서 Range 참조(reference) 저장 → `restoreSelection()` 내 `removeAllRanges()` 호출 후 Range 무효화 위험

**해결**: `sel.getRangeAt(0)` → `sel.getRangeAt(0).cloneRange()` 로 변경

```ts
// Before
selectionRange.current = sel.getRangeAt(0);
// After
selectionRange.current = sel.getRangeAt(0).cloneRange();
```

**관련 파일**: `src/app/my-shop/useAdFormState.ts`

---

### 1-12. Step3 T5(급구/추천) 색상 constants.ts 미수정 보완

**문제**: `constants.ts` DETAILED_PRICING에서 T5 `color: 'text-orange-600'` 로 남아 있어 Step3 카드에서 orange 표시

**해결**: `color: 'text-purple-600'` 으로 변경 (AD_TIER_STANDARDS purple 통일 기준 준수)

**관련 파일**: `src/app/my-shop/constants.ts`

---

## 4. 미완성 / 후속 과제

| 항목 | 상태 | 비고 |
|-----|------|------|
| 실제 텔레그램 ID 확정 | ⏳ 대기 | 현재 `@cocoalba` / `t.me/cocoalba` 플레이스홀더 |
| Supabase → shops.json 동기화 | ⏳ 미완 | 128개 샘플 공고 /jobs에서 미표시 |
| 등급별 노출 행수 규칙 | ⏳ 미착수 | PC/모바일 각 페이지별 티어당 몇 줄 노출할지 결정 필요 |
| Step2 에디터 iOS 실기기 검증 | ⏳ 대기 | 브라우저 emulation 적용 완료, 실 iPhone Safari 재확인 권장 |
