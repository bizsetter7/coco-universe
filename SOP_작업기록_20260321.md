# SOP 작업기록 — 2026-03-21
> P2 코코알바 브랜드 통합 시스템 | 작성자: Claude + 대표님
> 이 문서는 2026-03-21 세션의 전체 구현 내용, E2E 흐름, 안티그래비티 설계 결정을 기록합니다.

---

## 1. 이번 세션 구현 완료 목록

### 1-1. SOS 긴급구인 알림 시스템 (신규 구축)

| 파일 | 위치 | 내용 |
|------|------|------|
| `supabase/migrations/20260321_sos_system.sql` | 루트 | DB 마이그레이션 (실행 완료) |
| `src/lib/points.ts` | lib | SOS 포인트 reason 추가, getSosPointReason() |
| `public/sw.js` | 루트 | Web Push Service Worker (Stealth 정책) |
| `src/app/api/sos/subscribe/route.ts` | API | 구직자 Push 구독 등록/해제 |
| `src/app/api/sos/send/route.ts` | API | SOS 발송 (포인트차감 + 푸시) |
| `src/app/api/sos/count/route.ts` | API | 수신 가능 구직자 수 조회 |
| `src/app/api/sos/history/route.ts` | API | 발송 이력 조회 |
| `src/app/my-shop/components/SosAlertView.tsx` | 컴포넌트 | 업체용 SOS 발송 UI |
| `src/components/PushPermission.tsx` | 컴포넌트 | 구직자 Web Push 동의 UI |

### 1-2. 공고 등록 → 무통장 입금 안내 흐름 (신규)

| 파일 | 위치 | 내용 |
|------|------|------|
| `src/app/my-shop/components/BankTransferModal.tsx` | 컴포넌트 | 공고 저장 후 무통장 입금 안내 모달 |
| `src/app/my-shop/page.tsx` | 페이지 | handleSave → BankTransferModal 연결 |

### 1-3. 포인트 충전 UI (신규)

| 파일 | 위치 | 내용 |
|------|------|------|
| `src/app/my-shop/components/PointShopView.tsx` | 컴포넌트 | 포인트 패키지 선택 + 무통장 입금 안내 |
| `src/app/my-shop/components/BusinessSidebar.tsx` | 컴포넌트 | `💰 포인트 충전` 탭 추가 |

### 1-4. 점프이용권 (기존 UI → 실제 로직 연결)

| 파일 | 위치 | 내용 |
|------|------|------|
| `src/app/my-shop/components/OngoingAdsView.tsx` | 컴포넌트 | 점프 버튼 onJumpAd prop 연결 |
| `src/app/my-shop/page.tsx` | 페이지 | handleJump 구현 (500P 차감 + created_at 갱신) |

### 1-5. UI 수정

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 공고폼 하단 버튼 너비 | max-w-[1120px] 컨테이너 | max-w-[560px] 컨테이너 (절반) |
| 보기 버튼 | flex-1 md:w-40 | w-14 md:w-16 shrink-0 |
| 취소 버튼 | flex-1 md:w-32 | w-16 md:w-20 shrink-0 |
| 저장 및 심사저장 | "저장 및 심사 저장" | "저장 및 심사" |

---

## 2. E2E 흐름 (End-to-End)

### 2-1. 공고 등록 → 결제 → 심사 → 활성화 흐름

```
[업체 회원]
  └─ 마이페이지 진입
       └─ "새 공고 등록" 클릭
            └─ WarningModal (이용수칙 확인) → "확인 후 작성"
                 └─ Step1~4 작성
                      └─ 하단 총 신청금액 확인
                           └─ "저장 및 심사" 클릭
                                └─ Supabase shops INSERT (status='pending')
                                └─ Supabase payments INSERT (status='pending', method='bank_transfer')
                                └─ BankTransferModal 표시
                                     ├─ 토스뱅크 1002-4683-1712 표시
                                     ├─ 예금주: 고남우(초코아이디어)
                                     ├─ 계좌번호 복사 버튼
                                     └─ "확인" 클릭 → 대시보드 이동

[어드민]
  └─ 관리자 페이지 진입
       └─ "결제 내역" 탭
            └─ pending 상태 결제 목록 확인
                 └─ "승인하기" 클릭
                      └─ payments.status = 'completed'
                      └─ shops.status = 'active' + approved_at 설정
                           └─ 공고 즉시 노출 시작
```

**테스트 체크포인트:**
- [ ] totalAmount > 0 → BankTransferModal 표시
- [ ] totalAmount = 0 → "공고가 접수되었습니다" alert
- [ ] 수정(editingAdId 있음) → "공고가 수정되었습니다" alert + 바로 대시보드

---

### 2-2. SOS 긴급구인 발송 흐름

```
[이용 자격 확인]
  └─ SosAlertView 진입
       └─ Supabase 쿼리: shops WHERE user_id=X AND status='active' AND product_type IS NOT NULL
            ├─ 조건 미충족 → 🔒 잠금 화면 ("진행 중인 유료광고 없음")
            │    └─ "유료공고 등록하러 가기" → window.dispatchEvent('setView', 'form')
            └─ 조건 충족 → 발송 UI 표시

[SOS 발송 UI]
  └─ 지역 선택 (다중, 드롭다운)
       └─ 수신자 수 실시간 조회 (GET /api/sos/count?regions=...)
            └─ 예상 포인트 비용 표시
                 └─ 메시지 작성 (50자 이내)
                      └─ "SOS 발송" 클릭
                           └─ 포인트 잔액 확인
                                └─ confirm 다이얼로그 (지역/인원/포인트 표시)
                                     └─ POST /api/sos/send
                                          ├─ 수신자 조회 (push_subscriptions WHERE regions overlaps)
                                          ├─ 포인트 확인 & 차감
                                          ├─ point_logs 기록
                                          ├─ sos_alerts INSERT
                                          └─ Web Push 일괄 발송
                                               └─ 결과 표시 (성공/실패 카운트)
```

**포인트 차감 기준:**
| 수신자 수 | 비용 |
|-----------|------|
| 0~10명 | 200P |
| 11~30명 | 400P |
| 31~50명 | 600P |
| 51명 이상 | 800P |

**Stealth 정책 (구직자 프라이버시):**
- 잠금화면 알림 내용: "새 알림이 있습니다" (업소명/내용 미표시)
- 앱 열기 전까지 내용 확인 불가
- 업체에 구직자 위치 미전달
- Service Worker (`public/sw.js`)에서 처리

**테스트 체크포인트:**
- [ ] 유료공고 없는 계정 → 잠금 화면 표시
- [ ] 포인트 부족 → 오류 메시지 (alert)
- [ ] 50자 초과 메시지 → 발송 차단
- [ ] 구독자 없는 지역 → 발송 완료 (수신자 0명)
- [ ] 만료 구독(410 응답) → push_subscriptions 자동 삭제

---

### 2-3. 점프이용권 흐름

```
[업체 마이페이지 → 진행중인 채용정보]
  └─ 공고 카드의 "점프" 버튼 클릭
       └─ confirm("점프이용권 사용? 500P 차감")
            ├─ 취소 → 아무 동작 없음
            └─ 확인
                 └─ Supabase: profiles.points 조회
                      ├─ 잔액 부족 → alert + setView('buy-points')
                      └─ 잔액 충분
                           └─ profiles.points -= 500
                           └─ point_logs INSERT (reason='SHOP_JUMP')
                           └─ shops.created_at = NOW()
                                └─ fetchRegisteredAds() → 목록 갱신
                                └─ alert("점프이용권 적용됨!")
```

**테스트 체크포인트:**
- [ ] 500P 미만 → 부족 안내 + 포인트 충전 탭 이동
- [ ] 점프 후 공고 목록에서 해당 공고가 최상단으로 이동

---

### 2-4. 포인트 충전 흐름

```
[업체 마이페이지 → 💰 포인트 충전]
  └─ 5종 패키지 중 선택
       └─ 충전 신청 버튼 클릭
            └─ 무통장 입금 안내 표시
                 ├─ 토스뱅크 1002-4683-1712
                 ├─ 예금주: 고남우(초코아이디어)
                 └─ "업체명/닉네임 메모 필수" 안내

[어드민 → 수동 포인트 지급 예정]
  └─ (추후) 어드민 페이지 포인트 관리 탭에서 수동 지급
```

**포인트 패키지:**
| 패키지 | 포인트 | 가격 | 혜택 |
|--------|--------|------|------|
| 미니 | 2,000P | 10,000원 | - |
| 스몰 | 5,000P | 22,000원 | 10% 보너스 |
| 미디엄 | 12,000P | 49,000원 | 15% 보너스 |
| 라지 | 30,000P | 110,000원 | 20% 보너스 |
| 엑스라지 | 60,000P | 150,000원 | 25% 보너스 |

**주의:** 포인트 충전 신청 시 DB에 별도 기록 없음 (현재는 무통장 안내 UI만). 추후 `point_charge_requests` 테이블 추가 예정.

---

## 3. 안티그래비티 (Anti-Gravity) 설계 결정

> 안티그래비티 = 기술/법/비즈니스적 제약을 우회하거나 극복한 설계 결정들

### 3-1. 카카오 알림톡 → Web Push 전환
**배경:** 카카오 알림톡은 유흥업종 사업자 심사 통과 불가
**결정:** Web Push(VAPID) 방식으로 전면 교체
**Trade-off:** 앱 설치 불필요, 구독 동의 필요, 잠금화면 노출 가능

### 3-2. Stealth 정책 (구직자 프라이버시 최우선)
**배경:** 구직자 최대 거부감 = 주변인에게 유흥알바 사실 노출
**결정:** 잠금화면 알림 = "새 알림이 있습니다"만 표시, 업소명/내용 숨김
**구현:** `public/sw.js`에서 `stealth: true` 페이로드 처리
**신고 방식:** 알림 즉시 신고 없음 → 실제 출근 후 공고페이지/1:1문의만 (허위신고 방지)

### 3-3. 자동 입금 감지 불가 → 어드민 수동 승인
**배경:** 토스 개인계좌(1002-xxxx)는 API 웹훅 미지원
**결정:** 어드민 수동 `승인하기` → `payments=completed` + `shops=active`
**향후:** 토스 비즈니스 계정 전환 시 웹훅 연동 가능

### 3-4. GeoTargeting IP Fallback
**배경:** GPS 거부 사용자 = 위치 기반 서비스 불가
**결정:** `ipapi.co` API → 시/도 단위 fallback
**정렬 기준:** 내 지역 Grand/Premium → 내 지역 나머지 → 타지역

### 3-5. SOS 이용수칙 (유료공고 보유 조건)
**배경:** SOS를 무료 공고 업체가 남용하면 구직자 신뢰도 하락
**결정:** 진행 중 유료공고(T1~T7) 1개 이상 보유 시에만 SOS 사용 가능
**구현:** `shops WHERE status='active' AND product_type IS NOT NULL` 쿼리

### 3-6. 점프이용권 방식 (created_at 갱신)
**배경:** 별도 ranking 컬럼 없이 기존 정렬 활용
**결정:** `shops.created_at = NOW()` 업데이트 → 자연스럽게 최상단 정렬
**리스크 낮음:** 업체가 자신의 공고만 갱신 가능 (user_id 체크)

---

## 4. DB 스키마 (2026-03-21 추가)

### push_subscriptions
```sql
CREATE TABLE push_subscriptions (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id UUID UNIQUE NOT NULL REFERENCES profiles(id),
    subscription JSONB NOT NULL,        -- Web Push 구독 객체
    regions TEXT[] DEFAULT '{}',        -- 수신 동의 지역 배열
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### sos_alerts
```sql
CREATE TABLE sos_alerts (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    shop_id UUID NOT NULL REFERENCES profiles(id),
    shop_name TEXT NOT NULL,
    message TEXT NOT NULL CHECK (char_length(message) <= 50),
    target_regions TEXT[] NOT NULL,
    point_deducted INTEGER NOT NULL,
    recipient_count INTEGER NOT NULL DEFAULT 0,
    sent_at TIMESTAMPTZ DEFAULT NOW()
);
```

### point_logs (기존 테이블 확장)
```sql
-- reason 가능 값:
-- SOS_SEND_SMALL (-200P)
-- SOS_SEND_MEDIUM (-400P)
-- SOS_SEND_LARGE (-600P)
-- SOS_SEND_XLARGE (-800P)
-- SHOP_JUMP (-500P)
-- ADMIN_GRANT (+N P)
```

---

## 5. 환경변수 (.env.local)

```env
NEXT_PUBLIC_VAPID_PUBLIC_KEY=BHYfvErBfR-y-45xUGVLSFozuXgZpKYDWuSkx9Rk6TKLJLFgy0y6nXnbVTjzfcgaXx3pS9nbP1fifVLyz3EY0jc
VAPID_PRIVATE_KEY=[비공개]
VAPID_EMAIL=mailto:admin@cocoalba.com
SUPABASE_SERVICE_ROLE_KEY=[비공개]
```

---

## 6. 미완성 / 다음 작업 목록

| # | 항목 | 우선순위 | 비고 |
|---|------|----------|------|
| 1 | 포인트 충전 신청 DB 기록 | 높음 | `point_charge_requests` 테이블 필요 |
| 2 | 어드민 포인트 수동 지급 UI | 높음 | admin 페이지 새 탭 |
| 3 | PushPermission.tsx 레이아웃 삽입 | 중간 | 구직자 로그인 시 동의 요청 |
| 4 | GeoTargeting 내 지역 우선 정렬 실제 구현 | 중간 | shops 페이지 쿼리 수정 필요 |
| 5 | 공고 연장 기능 | 낮음 | OngoingAdsView 연장 버튼 핸들러 |
| 6 | 토스 비즈니스 웹훅 연동 | 낮음 | 계정 전환 후 |

---

## 7. 결제 계좌 정보

> **무통장 입금 유일 수단**
> - 은행: 토스뱅크
> - 계좌번호: 1002-4683-1712
> - 예금주: 고남우(초코아이디어)
> - 입금 메모에 업체명 또는 닉네임 필수
> - 처리 시간: 입금 확인 후 1~3시간 내
