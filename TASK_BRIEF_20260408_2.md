# TASK_BRIEF — 2026-04-08 (2차) — UI/데이터 6종 Fix

> **작성자**: 코부장 (Claude Code)
> **수신자**: Antigravity
> **우선순위**: HIGH — 실제 업체회원(뷰테라피 등) 광고 노출 품질 직결
> **관련 브랜치**: main (직접 수정 후 TASK_REPORT 파일 작성)

---

## ⚡ 시작 전 필독

```
□ C:\My-site\p2.브랜드_통합_시스템\CLAUDE.md 읽기
□ C:\My-site\p1.choco-idea\MISTAKES_LOG.md 읽기 (M-001~M-016)
□ 아래 작업 범위를 벗어나는 코드 절대 수정 금지
```

---

## 🎯 작업 배경

실제 업체회원(뷰테라피 외 3곳)이 등록한 광고(ID 143,144,145,148)가
- 상호명이 "관리자"로 표기됨
- 목업 광고 아래에 묻혀서 표시됨
- 광고상세팝업에서 헤더 색상이 관리자용과 일반 사용자용이 다름
- 키워드 섹션 타이틀이 잘못됨
- 지도가 표시 안 됨

총 6개 Fix를 이 브리프에서 한 번에 처리한다.

---

## 📋 Fix 목록

---

### Fix 1 — nickname 기본값 '관리자' 제거

**파일**: `src/app/my-shop/utils/normalization.ts`

**문제**: 현재 Line 35–36에서 nickname/managerNickname의 기본값이 `'관리자'`로 하드코딩되어,
nickname 미입력 업체의 상호명이 "관리자"로 표기됨.

**현재 코드** (Line 35–36):
```ts
nickname: getValid(ad.nickname, opt.nickname, '관리자'),
managerNickname: getValid(ad.nickname, opt.nickname, '관리자'),
```

**수정 후**:
```ts
nickname: getValid(ad.nickname, opt.nickname, ad.name || opt.shopName || ''),
managerNickname: getValid(ad.nickname, opt.nickname, ad.name || opt.shopName || ''),
```

**의도**: nickname이 없으면 상호명(name)으로 폴백. 상호명도 없으면 빈 문자열.
'관리자'라는 단어가 절대 기본값으로 노출되면 안 됨.

> `normalizePayment` 함수(Line 128~) 내 `defaultUserName` 파라미터는 **수정하지 말 것**.
> 그 함수는 결제 장부 표시용으로 별개 로직임.

---

### Fix 2 — 실제 업체 광고 정렬 우선순위 상향

**파일**: `src/app/page.tsx` (메인 페이지 클라이언트 사이드 정렬)

**문제**: 목업 광고(isMock=true 또는 user_id가 `6fc68887`로 시작)가 실제 광고보다 위에 표시됨.

**수정 지침**:
DB에서 받은 `dbShops` 배열을 렌더링하기 전에 정렬 로직 추가.
같은 tier(productType) 내에서는 `isMock === false` (= 실제 광고)가 항상 앞에 오도록 정렬.

```ts
// 기존 코드에서 dbShops를 사용하는 부분 직전에 삽입
const sortedShops = [...dbShops].sort((a, b) => {
  const aMock = a.isMock || String(a.user_id || '').startsWith('6fc68887') ? 1 : 0;
  const bMock = b.isMock || String(b.user_id || '').startsWith('6fc68887') ? 1 : 0;
  return aMock - bMock; // 실제 광고(0)가 앞으로
});
```

이후 `dbShops` 대신 `sortedShops` 를 컴포넌트에 전달.

**동일한 정렬 로직을**:
- `src/app/jobs/page.tsx` (서버 컴포넌트 → Supabase 쿼리에 `.order('user_id', ...)` 불가,
  대신 데이터 수신 후 `.sort()` 적용)
- `src/app/region/page.tsx` (동일 패턴)

에도 적용할 것. 단, 이 두 파일은 서버 컴포넌트이므로 `.sort()` 는 `return` 전 배열 처리로 적용.

---

### Fix 3 — 키워드 섹션 타이틀 복원

**파일**: `src/components/jobs/JobDetailModal.tsx`

**문제**: Line 363에 `추가 태그정보` 로 변경되어 있음. 원래 값은 `Keyword & 정보`.

**현재 코드** (Line 361–363):
```tsx
<h3 className="text-xs font-bold text-gray-400 flex items-center gap-1.5 opacity-80">
    <Info size={12} />
    추가 태그정보
</h3>
```

**수정 후**:
```tsx
<h3 className="text-xs font-bold text-gray-400 flex items-center gap-1.5 opacity-80">
    <Info size={12} />
    Keyword & 정보
</h3>
```

---

### Fix 4 — 자동 키워드 생성 로직 개선

**파일**: `src/components/jobs/JobDetailModal.tsx`

**문제**: Line 368에서 `generateSEOKeywords(shop.region)` 만 호출하여 업체명이 빠진
의미 없는 키워드만 생성됨.

**현재 코드** (Line 367–369):
```ts
const autoKeywords = generateSEOKeywords(shop.region);
const userKeywords = shop.options?.keywords || [];
```

**수정 후**:
```ts
const userKeywords: string[] = Array.isArray(shop.options?.keywords) ? shop.options.keywords : [];

// userKeywords가 없을 때만 자동 생성
const autoKeywords: string[] = (() => {
    if (userKeywords.length > 0) return []; // 사용자 키워드 있으면 자동생성 불필요
    const city = shop.options?.regionCity || (typeof shop.region === 'string' ? shop.region.split(' ')[0] : '');
    const gu = shop.options?.regionGu || '';
    const shopNm = shop.name || shop.shopName || '';
    const cat = shop.category || '';
    const combos: string[] = [];
    if (city && shopNm) combos.push(`${city}${shopNm}`);
    if (city && gu && shopNm) combos.push(`${city}${gu}${shopNm}`);
    if (city && cat) combos.push(`${city}${cat}알바`);
    if (gu && shopNm) combos.push(`${gu}${shopNm}`);
    if (city) combos.push(`${city}여자알바`);
    return combos.filter(k => k.trim().length > 2);
})();
```

---

### Fix 5 — 지도 Mapbox → Kakao Map 교체

**파일**: `src/components/jobs/JobDetailModal.tsx`

**문제**: Line 343에서 Mapbox 정적 이미지 URL에 `shop.lng || 126.9780` 하드코딩 fallback 사용.
Kakao Map SDK는 `MobilePreviewContent.tsx`에 이미 완성된 패턴이 있음.

**수정 지침**:

1. **파일 상단 import 추가**:
```ts
import { useRef, useEffect, useState } from 'react'; // 이미 있으면 스킵
```

2. **`loadKakaoMapSdk` 함수**: `MobilePreviewContent.tsx` 상단(Line 11–79)의 함수를 그대로 복사하여
   `JobDetailModal.tsx` 최상단 (컴포넌트 정의 바깥)에 붙여넣기.

3. **`JobDetailContent` 컴포넌트 내부에 상태 및 ref 추가**:
```ts
const mapContainerRef = useRef<HTMLDivElement>(null);
const [mapError, setMapError] = useState<string | null>(null);
const [resolvedAddress, setResolvedAddress] = useState<string | null>(publisherAddress || null);
```

4. **useEffect 추가** (지도 렌더링 — `MobilePreviewContent.tsx` Line 115–144 패턴 동일하게):
```ts
useEffect(() => {
    // publisherAddress가 이미 있으면(prop으로 전달된 경우) 바로 사용
    if (publisherAddress) { setResolvedAddress(publisherAddress); return; }
    // 없으면 profiles에서 조회
    const userId = shop.user_id;
    if (!userId) return;
    import('@/lib/supabase').then(({ supabase }) => {
        supabase.from('profiles')
            .select('business_address, business_address_detail')
            .eq('id', userId)
            .single()
            .then(({ data }) => {
                if (data?.business_address) {
                    const detail = (data as any).business_address_detail;
                    setResolvedAddress(detail ? `${data.business_address} ${detail}` : data.business_address);
                }
            });
    });
}, [shop.user_id, publisherAddress]);

// 카카오 지도 렌더링 (MobilePreviewContent와 동일 패턴)
useEffect(() => {
    if (!resolvedAddress || !mapContainerRef.current) return;
    let cancelled = false;
    loadKakaoMapSdk()
        .then(() => {
            if (cancelled || !mapContainerRef.current) return;
            const kakao = (window as any).kakao;
            const geocoder = new kakao.maps.services.Geocoder();
            geocoder.addressSearch(resolvedAddress, (result: any[], status: string) => {
                if (cancelled || !mapContainerRef.current) return;
                if (status !== kakao.maps.services.Status.OK || !result[0]) {
                    setMapError('주소를 지도에서 찾을 수 없습니다.');
                    return;
                }
                const coords = new kakao.maps.LatLng(result[0].y, result[0].x);
                const map = new kakao.maps.Map(mapContainerRef.current, { center: coords, level: 4 });
                new kakao.maps.Marker({ map, position: coords });
            });
        })
        .catch(err => { if (!cancelled) setMapError(err.message || '지도를 불러오지 못했습니다.'); });
    return () => { cancelled = true; };
}, [resolvedAddress]);
```

5. **지도 렌더링 JSX 교체** (Line 341–356 영역, `<img src="https://api.mapbox.com...">` 블록 전체):
```tsx
<div
    className="aspect-video rounded-xl bg-gray-100 border border-gray-50 overflow-hidden relative"
>
    {resolvedAddress ? (
        mapError ? (
            <div className="absolute inset-0 flex flex-col items-center justify-center gap-2 text-gray-400">
                <MapPin size={28} className="opacity-40" />
                <span className="text-xs font-medium">{mapError}</span>
            </div>
        ) : (
            <div ref={mapContainerRef} className="absolute inset-0 w-full h-full" />
        )
    ) : (
        <div className="absolute inset-0 flex flex-col items-center justify-center gap-2 text-gray-400">
            <MapPin size={28} className="opacity-40" />
            <span className="text-xs font-medium">위치 정보를 불러오는 중...</span>
        </div>
    )}
    <div className="absolute bottom-0 inset-x-0 bg-white/95 backdrop-blur-sm p-3 border-t border-gray-100 flex items-center gap-3 z-10">
        <MapPin size={24} className="text-gray-400 shrink-0" />
        <div>
            <div className="text-[12px] font-black text-gray-900">사업자 등록 주소</div>
            <div className="text-[11px] text-gray-500 font-medium">
                {resolvedAddress || '주소 정보 없음'}
            </div>
        </div>
    </div>
</div>
```

---

### Fix 6 — 헤더 그라데이션 통일

**파일**: `src/components/jobs/JobDetailModal.tsx`

**문제**: `getHeaderTheme()` 함수의 gradient 값이 `MobilePreviewContent.tsx`의 `TIER_STYLE_MAP`과 다름.
실제 사용자가 보는 팝업(`JobDetailModal`)과 관리자 팝업(`MobilePreviewContent`)의 색상이 달라 혼선.

**기준**: `MobilePreviewContent.tsx`의 `TIER_STYLE_MAP`을 **정답**으로 간주.

**현재 JobDetailModal.tsx** `getHeaderTheme()` (Line 62–72):
```ts
case 'p1': return { bg: "from-amber-500 to-amber-600",     ... };
case 'p2': return { bg: "from-red-600 to-red-700",         ... };
case 'p3': return { bg: "from-blue-600 to-blue-700",       ... };
case 'p4': return { bg: "from-emerald-600 to-emerald-700", ... };
case 'p5': return { bg: "from-purple-600 to-purple-700",   ... };
case 'p6': return { bg: "from-slate-600 to-slate-700",     ... };
default:   return { bg: "from-stone-700 to-stone-800",     ... };
```

**수정 후** (MobilePreviewContent 기준으로 통일 — accent/badge는 유지, bg만 교체):
```ts
case 'p1': return { bg: "from-amber-400 via-orange-500 to-amber-600",     accent: "text-amber-500",   badge: "bg-white/20" };
case 'p2': return { bg: "from-red-500 to-rose-700",                        accent: "text-red-600",     badge: "bg-white/20" };
case 'p3': return { bg: "from-blue-500 to-indigo-600",                     accent: "text-blue-600",    badge: "bg-white/20" };
case 'p4': return { bg: "from-emerald-400 to-teal-600",                    accent: "text-emerald-600", badge: "bg-white/20" };
case 'p5': return { bg: "from-purple-500 to-violet-600",                   accent: "text-purple-500",  badge: "bg-white/20" };
case 'urgent': return { bg: "from-purple-500 to-violet-600",               accent: "text-purple-500",  badge: "bg-white/20" };
case 'p6': return { bg: "from-slate-400 to-gray-600",                      accent: "text-slate-500",   badge: "bg-white/20" };
default:   return { bg: "from-slate-300 to-slate-400",                     accent: "text-stone-400",   badge: "bg-white/20" };
```

---

## 📝 완료 보고 요령

1. `TASK_REPORT_20260408_2.md` 파일을 P2 프로젝트 루트에 생성
2. Fix 번호별로 "수정 파일", "수정 라인", "수정 내용 한 줄 요약" 기록
3. 빌드 오류 없는지 `npm run build` 확인 후 에러 있으면 보고에 포함
4. 커밋은 코부장 검수 후 진행 (직접 커밋 금지)

---

## ⚠️ 금지 사항

- `normalizePayment` 함수 수정 금지 (결제 장부 별도 로직)
- `AD_TIER_STANDARDS` 배열 값 수정 금지 (PROTECTED)
- `shops.json` 수정 금지 (정적 fallback, 건드리지 말 것)
- 이 브리프에 없는 파일 임의 수정 금지
